<%- include('partials/header', {
  titulo: 'Backup do Sistema',
  subtitulo: 'Faça backup dos seus dados',
  user: locals.user
}) %>

<div class="card">
  <h2>🔒 Backup de Dados</h2>
  
  <% if (message) { %>
    <div class="alert <%= message.type === 'error' ? 'alert-danger' : 'alert-success' %>">
      <%= message.text %>
    </div>
  <% } %>
  
  <p>Clique no botão abaixo para gerar um arquivo de backup com todos os dados do sistema.</p>
  
  <form action="/backup/gerar" method="POST">
    <button type="submit" class="btn btn-primary">
      📥 Gerar Backup
    </button>
  </form>
  
  <div class="info-box" style="margin-top: 20px;">
    <h3>ℹ️ Informações sobre o Backup</h3>
    <ul>
      <li>O backup incluirá todas as tabelas do sistema</li>
      <li>O arquivo será baixado em formato SQL</li>
      <li>Guarde o arquivo em local seguro</li>
      <li>Use este arquivo para restaurar os dados em caso de necessidade</li>
    </ul>
  </div>
</div>

<style>
.info-box {
  background: #f0f0f0;
  padding: 15px;
  border-radius: 5px;
}

.alert {
  padding: 10px;
  margin: 10px 0;
  border-radius: 5px;
}

.alert-danger {
  background: #f8d7da;
  color: #721c24;
  border: 1px solid #f5c6cb;
}

.alert-success {
  background: #d4edda;
  color: #155724;
  border: 1px solid #c3e6cb;
}
</style>

<%- include('partials/footer') %>