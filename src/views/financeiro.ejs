<%- include('partials/header', {
  titulo: 'Financeiro',
  subtitulo: 'Visão geral do fluxo de caixa',
  user: locals.user
}) %>

<div class="nav">
    <a href="/financeiro/completo">📊 Fluxo de Caixa</a>
    <a href="/financeiro/faturamento">🧾 Faturamento</a>
    <a href="/financeiro/dre">📈 DRE</a>
</div>

<div class="stats">
  <div class="stat-card">
    <h3>R$ <%= (totais.total_credito || 0).toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></h3>
    <p>Total de Entradas</p>
  </div>
  <div class="stat-card">
    <h3>R$ <%= (totais.total_debito || 0).toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></h3>
    <p>Total de Saídas</p>
  </div>
  <div class="stat-card <%= saldoAtual >= 0 ? '' : 'saldo-negativo' %>">
    <h3>R$ <%= saldoAtual.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></h3>
    <p>Saldo Atual</p>
  </div>
</div>

<div class="card">
  <h2>➕ Novo Lançamento Avulso</h2>
  <form action="/financeiro/lancamento" method="POST">
    <div class="form-row">
      <div class="form-group">
        <label for="data_operacao">Data *</label>
        <input type="date" id="data_operacao" name="data_operacao" value="<%= hoje %>" required>
      </div>
      <div class="form-group">
        <label for="tipo">Tipo *</label>
        <select id="tipo" name="tipo" required>
          <option value="">Selecione</option>
          <option value="CREDITO">💰 Entrada (Crédito)</option>
          <option value="DEBITO">💸 Saída (Débito)</option>
        </select>
      </div>
    </div>
    <div class="form-row">
      <div class="form-group">
        <label for="valor">Valor (R$) *</label>
        <input type="number" id="valor" name="valor" step="0.01" min="0.01" required>
      </div>
      <div class="form-group">
        <label for="descricao">Descrição *</label>
        <input type="text" id="descricao" name="descricao" required>
      </div>
    </div>
    <button type="submit" class="btn">Registrar Lançamento</button>
  </form>
</div>

<div class="card">
  <h2>📋 Últimos Lançamentos no Caixa</h2>
  <table class="table">
    <thead>
      <tr>
        <th>Data</th>
        <th>Tipo</th>
        <th>Descrição</th>
        <th class="text-right">Valor</th>
      </tr>
    </thead>
    <tbody>
      <% if (contas.length > 0) { %>
        <% contas.forEach(item => { %>
          <tr>
            <td><%= new Date(item.data_operacao).toLocaleDateString('pt-BR', {timeZone: 'UTC'}) %></td>
            <td><span class="badge <%= item.tipo === 'CREDITO' ? 'badge-success' : 'badge-danger' %>"><%= item.tipo %></span></td>
            <td><%= item.descricao %></td>
            <td class="text-right <%= item.tipo === 'CREDITO' ? 'saldo-positivo' : 'saldo-negativo' %>">
              <%= item.tipo === 'CREDITO' ? '+' : '-' %> R$ <%= parseFloat(item.valor).toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
            </td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr>
          <td colspan="4" class="text-center">Nenhum lançamento encontrado.</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

<%- include('partials/footer') %>