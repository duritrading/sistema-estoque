<%- include('partials/header', {
  titulo: 'Financeiro',
  subtitulo: 'Visão geral do fluxo de caixa',
  user: locals.user
}) %>

<div class="nav">
    <a href="/financeiro/completo">📊 Fluxo de Caixa</a>
    <a href="/financeiro/faturamento">🧾 Faturamento</a>
    <a href="/financeiro/dre">📈 DRE</a>
</div>

<div class="stats">
  <div class="stat-card">
    <h3>R$ <%= (totais.total_credito || 0).toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></h3>
    <p>Total de Entradas</p>
  </div>
  <div class="stat-card">
    <h3>R$ <%= (totais.total_debito || 0).toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></h3>
    <p>Total de Saídas</p>
  </div>
  <div class="stat-card <%= saldoAtual >= 0 ? '' : 'saldo-negativo' %>">
    <h3>R$ <%= saldoAtual.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %></h3>
    <p>Saldo Atual</p>
  </div>
</div>

<div class="card">
  <h2>➕ Novo Lançamento Avulso</h2>
  <form action="/financeiro/lancamento" method="POST">
    <div class="form-row">
      <div class="form-group">
        <label for="data_operacao">Data *</label>
        <input type="date" id="data_operacao" name="data_operacao" value="<%= hoje %>" required>
      </div>
      <div class="form-group">
        <label for="tipo">Tipo *</label>
        <select id="tipo" name="tipo" required>
          <option value="">Selecione</option>
          <option value="CREDITO">💰 Entrada (Crédito)</option>
          <option value="DEBITO">💸 Saída (Débito)</option>
        </select>
      </div>
    </div>
    <div class="form-row">
      <div class="form-group">
        <label for="valor">Valor (R$) *</label>
        <input type="number" id="valor" name="valor" step="0.01" min="0.01" required>
      </div>
      <div class="form-group">
        <label for="descricao">Descrição *</label>
        <input type="text" id="descricao" name="descricao" required>
      </div>
    </div>
    <button type="submit" class="btn">Registrar Lançamento</button>
  </form>
</div>

<div class="card">
  <h2>📋 Últimos Lançamentos no Caixa</h2>
  <table class="table mt-2">
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Produto</th> <th>Vencimento</th>
      <th>Parcela</th>
      <th class="text-right">Valor</th>
      <th>Status</th>
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    <% if (contas.length === 0) { %>
      <tr>
        <td colspan="7" class="text-center">Nenhuma conta a receber encontrada para o período selecionado.</td>
      </tr>
    <% } else { %>
      <% contas.forEach(conta => { %>
        <tr>
          <td><%= conta.cliente_nome %></td>
          <td><%= conta.produto_descricao %></td> <td><%= new Date(conta.data_vencimento).toLocaleDateString('pt-BR', { timeZone: 'UTC' }) %></td>
          <td><%= conta.numero_parcela %> / <%= conta.total_parcelas %></td>
          <td class="text-right">R$ <%= parseFloat(conta.valor).toLocaleString('pt-BR', { minimumFractionDigits: 2 }) %></td>
          <td>
            <% if (conta.status === 'Pago') { %>
              <span class="badge badge-success">Pago</span>
            <% } else if (new Date(conta.data_vencimento) < new Date() && conta.status !== 'Pago') { %>
              <span class="badge badge-danger">Atrasado</span>
            <% } else { %>
              <span class="badge badge-warning">Pendente</span>
            <% } %>
          </td>
          <td>
            <% if (conta.status !== 'Pago') { %>
              <form action="/financeiro/faturamento/registrar-pagamento/<%= conta.id %>" method="POST" onsubmit="return confirm('Confirma o recebimento desta parcela?');">
                <button type="submit" class="btn btn-success" style="padding: 5px 10px; font-size: 12px;">Registrar Pagamento</button>
              </form>
            <% } else { %>
              -
            <% } %>
          </td>
        </tr>
      <% }) %>
    <% } %>
  </tbody>
  <tfoot>
    <% const totalPagina = contas.reduce((sum, item) => sum + (item.tipo === 'CREDITO' ? parseFloat(item.valor) : -parseFloat(item.valor)), 0); %>
    <tr style="font-weight: bold; background-color: #f8f9fa;">
        <td colspan="3" class="text-right">Balanço na Página:</td>
        <td class="text-right <%= totalPagina >= 0 ? 'saldo-positivo' : 'saldo-negativo' %>">
            R$ <%= totalPagina.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
        </td>
    </tr>
</tfoot>
</table>
</div>

<%- include('partials/footer') %>