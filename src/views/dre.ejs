<%- include('partials/header', {
  titulo: 'DRE - Demonstrativo de Resultado',
  subtitulo: `Visão geral para o ano de ${ano}`,
  user: locals.user
}) %>

</div>

<div class="card">
  <h2>📈 DRE Gerencial - <%= ano %></h2>
  <div style="overflow-x: auto;">
    <table class="table mt-2">
      <thead>
        <tr>
          <th>Descrição</th>
          <th class="text-right">Total Ano</th>
        </tr>
      </thead>
      <tbody>
        <% estrutura.forEach(linha => { %>
          <% const valorFinal = resultados[linha.label] || 0; %>
          <tr class="<%= linha.css %> <%= linha.tipo === 'header' ? 'dre-header-row' : '' %>">
            <td>
              <span class="<%= linha.tipo === 'item' ? 'dre-sub-item' : '' %>">
                <%= linha.label %>
              </span>
            </td>
            <td class="text-right <%= valorFinal < 0 ? 'saldo-negativo' : '' %>">
              <% if (linha.tipo === 'total' || linha.tipo === 'header') { %>
                <strong><%= valorFinal !== 0 ? `R$ ${valorFinal.toLocaleString('pt-BR', {minimumFractionDigits: 2})}` : '-' %></strong>
              <% } else { %>
                <%= valorFinal !== 0 ? `R$ ${valorFinal.toLocaleString('pt-BR', {minimumFractionDigits: 2})}` : '-' %>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<%- include('partials/footer') %>